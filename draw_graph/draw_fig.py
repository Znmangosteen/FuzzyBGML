import matplotlib.pyplot as plt

def splashes_fig():
    pass

'''
        fig_path = './运行结果/' + data_set + '/figure/'
        exist_draw = [int(x[:-4].split(' ')[0]) for x in os.listdir(fig_path)]
        last_draw = max(exist_draw) if exist_draw else 0

        draw_as = path + '{0} f {1} g {2} s {3} e {4}.txt'.format(last_draw + 1, 1, (i + 1) * gen_num, size, 0)
        splashes_fig(fig_path, write_as,  )
'''